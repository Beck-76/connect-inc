<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Connect Inc. Chat Room</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f0f4f8;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      height: 100vh;
    }
    header {
      background: #bf0a30; /* Patriotic red */
      color: white;
      padding: 1rem;
      text-align: center;
      font-weight: bold;
      font-size: 1.5rem;
      box-shadow: 0 4px 6px rgba(0,0,0,0.2);
      font-family: 'Georgia', serif;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      user-select: none;
    }
    #chatContainer {
      flex: 1;
      display: flex;
      flex-direction: column;
      max-width: 600px;
      margin: 1rem auto;
      background: white;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      overflow: hidden;
      border: 4px solid #003366; /* Patriotic navy border */
    }
    #messages {
      flex: 1;
      padding: 1rem;
      overflow-y: auto;
      border-bottom: 1px solid #ccc;
    }
    .message {
      margin-bottom: 1rem;
      max-width: 80%;
      padding: 0.5rem 1rem;
      border-radius: 15px;
      clear: both;
      word-wrap: break-word;
      font-size: 1rem;
      line-height: 1.3;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .user {
      background: #dbe9ff; /* lighter patriotic blue */
      float: right;
      text-align: right;
      color: #003366;
      font-weight: 600;
      box-shadow: 1px 1px 3px rgba(0,0,0,0.1);
    }
    .bot {
      background: #f5f7fa;
      float: left;
      text-align: left;
      color: #333;
      font-weight: 500;
      box-shadow: 1px 1px 3px rgba(0,0,0,0.05);
    }
    #inputArea {
      display: flex;
      padding: 1rem;
      background: #f7f9fb;
      border-top: 2px solid #003366;
    }
    #messageInput {
      flex: 1;
      padding: 0.5rem 1rem;
      border: 2px solid #003366;
      border-radius: 20px;
      font-size: 1rem;
      outline: none;
      transition: border-color 0.3s ease;
    }
    #messageInput:focus {
      border-color: #bf0a30;
      box-shadow: 0 0 5px #bf0a30;
    }
    #sendBtn {
      background: #003366;
      color: white;
      border: none;
      padding: 0 1.5rem;
      margin-left: 0.5rem;
      border-radius: 20px;
      cursor: pointer;
      font-weight: bold;
      font-size: 1rem;
      transition: background-color 0.3s ease;
      font-family: 'Georgia', serif;
      text-transform: uppercase;
      letter-spacing: 1px;
      box-shadow: 0 3px 6px rgba(0,0,0,0.3);
      user-select: none;
    }
    #sendBtn:hover {
      background: #bf0a30;
      box-shadow: 0 4px 8px rgba(191,10,48,0.8);
    }
    #typingIndicator {
      padding: 0.5rem 1rem;
      font-style: italic;
      color: #666;
      height: 20px;
      clear: both;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
  </style>
</head>
<body>
  <header>Connect Inc. Chat Room</header>
  <div id="chatContainer">
    <div id="messages"></div>
    <div id="typingIndicator"></div>
    <div id="inputArea">
      <input type="text" id="messageInput" placeholder="Ask CivBot about U.S. government, civics, voting..." autocomplete="off" />
      <button id="sendBtn">Send</button>
    </div>
  </div>

  <script>
    const messages = document.getElementById('messages');
    const messageInput = document.getElementById('messageInput');
    const sendBtn = document.getElementById('sendBtn');
    const typingIndicator = document.getElementById('typingIndicator');

    function addMessage(text, isBot) {
      const messageDiv = document.createElement('div');
      messageDiv.classList.add('message');
      messageDiv.classList.add(isBot ? 'bot' : 'user');
      messageDiv.textContent = text;
      messages.appendChild(messageDiv);
      messages.scrollTop = messages.scrollHeight;
    }

    function showTyping() {
      typingIndicator.textContent = 'CivBot is typing...';
    }

    function hideTyping() {
      typingIndicator.textContent = '';
    }

    function getBotResponse(message) {
      const civicsFacts = [
        "The Declaration of Independence was adopted on July 4, 1776.",
        "The U.S. Constitution was signed on September 17, 1787.",
        "The Bill of Rights contains the first 10 amendments to the Constitution.",
        "The Federalist Papers helped promote ratification of the Constitution.",
        "The Great Compromise created a bicameral legislature in Congress.",
        "The 19th Amendment granted women the right to vote in 1920.",
        "The Electoral College formally elects the President of the United States.",
        "The Emancipation Proclamation freed slaves in Confederate states during the Civil War.",
        "The Supreme Court case Marbury v. Madison established judicial review.",
        "Checks and balances prevent any one branch of government from becoming too powerful."
      ];

      const msg = message.toLowerCase().trim();

      if (msg.includes('president')) {
        return "The current President of the United States is Donald J. Trump, inaugurated on January 20, 2025.";
      }
      if (msg.includes('vice president')) {
        return "The Vice President is Kamala Harris. She presides over the Senate and is first in line to the presidency.";
      }
      if (msg.includes('speaker of the house')) {
        return "Mike Johnson is the current Speaker of the House of Representatives.";
      }
      if (msg.includes('chief justice')) {
        return "John Roberts is the Chief Justice of the U.S. Supreme Court.";
      }
      if (msg.includes('supreme court')) {
        return "The Supreme Court has 9 justices and interprets laws under the Constitution.";
      }
      if (msg.includes('bill of rights')) {
        return "The Bill of Rights is the first 10 amendments to the Constitution, ensuring personal freedoms.";
      }
      if (msg.includes('three branches') || msg.includes('branches of government')) {
        return "The U.S. government has three branches: Legislative, Executive, and Judicial.";
      }
      if (msg.includes('how do i vote') || msg.includes('voting')) {
        return "To vote, you must be a U.S. citizen, 18+, and registered in your state. Visit vote.gov for info.";
      }
      if (msg.includes('fact')) {
        return civicsFacts[Math.floor(Math.random() * civicsFacts.length)];
      }
      if (msg.includes('who are you') || msg.includes('your name')) {
        return "I'm CivBot 🇺🇸 — your friendly civic tech assistant!";
      }
      return "I'm CivBot 🇺🇸 — here to help with questions about U.S. government, civics, and voting!";
    }

    sendBtn.addEventListener('click', () => {
      const userMessage = messageInput.value.trim();
      if (!userMessage) return;
      addMessage(userMessage, false);
      messageInput.value = '';
      showTyping();
      setTimeout(() => {
        hideTyping();
        const botReply = getBotResponse(userMessage);
        addMessage(botReply, true);
      }, 1000);
    });

    messageInput.addEventListener('keypress', function (e) {
      if (e.key === 'Enter') sendBtn.click();
    });
  </script>
</body>
</html>
