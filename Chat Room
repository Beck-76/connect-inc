<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Chat Room - Connect Inc.</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet"/>
  <style>
    body {
      margin: 0;
      font-family: 'Roboto', sans-serif;
      background: url('https://upload.wikimedia.org/wikipedia/commons/6/6e/Flag_of_the_United_States_%28watermark_style%29.svg') no-repeat center center fixed;
      background-size: cover;
      color: #000;
    }

    header {
      background: #002868;
      color: white;
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    header h1 {
      font-size: 1.8rem;
      margin: 0;
    }

    nav.menu {
      display: flex;
      gap: 1rem;
    }

    nav.menu a {
      color: white;
      text-decoration: none;
      font-weight: bold;
    }

    nav.menu a:hover {
      text-decoration: underline;
    }

    main {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 2rem;
    }

    .chat-container {
      background-color: rgba(255, 255, 255, 0.9);
      border: 2px solid #002868;
      border-radius: 10px;
      width: 100%;
      max-width: 600px;
      height: 500px;
      display: flex;
      flex-direction: column;
      padding: 1rem;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .chat-log {
      flex-grow: 1;
      border: 1px solid #ccc;
      border-radius: 5px;
      padding: 1rem;
      overflow-y: auto;
      background: #fff;
      margin-bottom: 1rem;
    }

    .chat-input {
      display: flex;
      gap: 0.5rem;
    }

    .chat-input input {
      flex-grow: 1;
      padding: 0.75rem;
      font-size: 1rem;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    .chat-input button {
      padding: 0.75rem 1rem;
      background-color: #002868;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    .chat-input button:hover {
      background-color: #001f4d;
    }

    footer {
      background-color: #002868;
      color: white;
      text-align: center;
      padding: 1rem;
      font-size: 0.9rem;
      margin-top: 2rem;
    }
  </style>
</head>
<body>
  <header>
    <h1>Connect Chat Room</h1>
    <nav class="menu">
      <a href="index.html">Home</a>
      <a href="house.html">House</a>
      <a href="senate.html">Senate</a>
    </nav>
  </header>

  <main>
    <div class="chat-container">
      <div class="chat-log" id="chatLog">
        <p><em>Welcome to the civic chat room! Be respectful and keep it civil 🇺🇸</em></p>
      </div>
      <div class="chat-input">
        <input type="text" id="messageInput" placeholder="Type your message..."/>
        <button onclick="sendMessage()">Send</button>
      </div>
    </div>
  </main>

  <footer>
    Connect Inc. • Civic Conversations for the People
  </footer>

  <script>
    const chatLog = document.getElementById("chatLog");
    const input = document.getElementById("messageInput");

    function sendMessage() {
      const message = input.value.trim();
      if (message !== "") {
        const msgElem = document.createElement("p");
        msgElem.textContent = `You: ${message}`;
        chatLog.appendChild(msgElem);
        chatLog.scrollTop = chatLog.scrollHeight;
        input.value = "";
      }
    }

    input.addEventListener("keypress", function (e) {
      if (e.key === "Enter") {
        sendMessage();
      }
    });
  </script>
</body>
</html>
